<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/MyDefaultButton</item>
        <item name="android:textViewStyle">@style/MyDefaultTitle</item>
    </style>

    <style name="MyDefaultButton" parent="android:Widget.Holo.Button" >
        <item name="android:background">#F08C35</item>
        <item name="android:padding">@dimen/activity_vertical_margin</item>
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_marginTop">24dp</item>
        <item name="android:width">260dp</item>
        <item name="android:textColor">@android:color/black</item>
        <!--<item name="android:background">@drawable/button_selector</item>-->
    </style>
    <style name="MyDefaultTitle" parent="android:Widget.Holo.Light.TextView">
        <item name="android:textColor">@android:color/holo_blue_dark</item>
        <item name="android:textSize">30dp</item>
    </style>
    <style name="MyCapstonButton" parent="@style/MyDefaultButton">
        <item name="android:background">@android:color/holo_red_dark</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>
